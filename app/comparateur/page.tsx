import Link from 'next/link'
import { ArrowLeft } from 'lucide-react'
import { products, formatPrice } from '@/data/products'
export default function Compare(){return <main className="min-h-screen bg-background"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-7 lg:px-10"><Link href="/" className="font-display text-2xl tracking-[.16em]">DORFLEX</Link><Link href="/nos-matelas" className="text-sm text-muted-foreground"><ArrowLeft className="mr-2 inline size-4"/>Ajouter un matelas</Link></header><section className="mx-auto max-w-7xl px-6 py-14 lg:px-10"><p className="text-xs font-semibold uppercase tracking-[.25em] text-primary">Votre sélection</p><h1 className="mt-4 font-display text-5xl md:text-7xl">Comparer les matelas</h1><div className="mt-12 overflow-x-auto rounded-2xl border border-border bg-card"><table className="w-full min-w-[650px] text-left text-sm"><thead><tr className="border-b border-border"><th className="p-5 font-normal text-muted-foreground">Produit</th>{products.slice(0,3).map(p=><th key={p.slug} className="p-5 font-display text-xl">{p.name}</th>)}</tr></thead><tbody>{[['Technologie','technology'],['Fermeté','firmness'],['Hauteur','height'],['Prix','price']].map(([label,key])=><tr key={label} className="border-b border-border last:border-0"><td className="p-5 text-muted-foreground">{label}</td>{products.slice(0,3).map(p=><td key={p.slug} className="p-5">{key==='price'?formatPrice(p.price):p[key as 'technology'|'firmness'|'height']}</td>)}</tr>)}</tbody></table></div></section></main>}
